# IDE / OS (repo root)
.idea/
.DS_Store
Thumbs.db

# Cursor — 로컬 전용 (규칙·에이전트 설정 등 저장소에 포함하지 않음)
.cursor/
**/.cursor/
.cursorrules
.cursorignore
AGENTS.md

logs/

# 서버 전용 배포 훅 (예시는 deploy.local.sh.example)
scripts/deploy.local.sh

# Node.js / pnpm (repo root)
node_modules/
.pnpm-debug.log*
.pnpm-store/
pnpm-lock.yaml