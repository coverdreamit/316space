#!/usr/bin/env bash
# 선택: deploy.local.sh 로 복사 후 수정 (deploy.local.sh 는 .gitignore 로 커밋 안 함)
#
# deploy-pull-restart.sh 는 git pull 후 저장소 루트에서 docker compose up -d --build 만 수행합니다.
# 배포 직후 추가 단계(DB 마이그레이션, 캐시 무효화 등)가 필요하면 아래에 정의하세요.

# restart_app_services() {
#   echo "post-deploy hook"
# }
